# Run with gradle
```bash
./gradlew runInvestigation --args='-l java --resume'
```

# Build distribution
```bash
./gradlew installDist
```

# Run with script
Make sure tmux is installed.
```bash
sh ./script/investigation.sh
```